{
  "name": "worker",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "npx wrangler dev --ip 0.0.0.0 --port 8787",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "wrangler": "4.80.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "typescript": "5.3.3"
  }
}
